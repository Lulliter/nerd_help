# Configuration in R x HTTPS ACCREDITATION ----------------------------------------------------------
# Required pckgs ----------------------------------------------------------
if (!require("pacman")) {
	install.packages('pacman' )
}

pacman::p_load(usethis, gitcreds, gh)

